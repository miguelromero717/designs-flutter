import 'package:flutter/material.dart';

class ButtonsDesignPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Buttons Page'),
        ),
      ),
    );
  }
}